# Lane claim: <short name>

- **Agent:** <name or role>
- **State:** `OPEN`
- **Objective:** <bounded outcome>
- **Claimed paths:**
  - `<repository-relative-path>`
- **Read-only inputs:**
  - `<repository-relative-path or public document>`
- **Expected evidence:** <tests, review, or documentation check>
- **Boundary:** <what this lane must not change>

Add a concise row to [../LANES.md](../LANES.md) before editing the claimed
paths. Update the state to `CLOSED` or `BLOCKED` when the work stops.
