# Application source boundary

Put this project’s application source here, or replace this directory with the
layout appropriate for its language and framework.

This starter intentionally contains no Conductor Relay backend code, generated
client, credential, or network implementation. Keep application-specific
architecture in `docs/ARCHITECTURE.md` and update `docs/PROJECT.md` as the
project gains defined requirements.
